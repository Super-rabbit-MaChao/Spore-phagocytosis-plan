## 新增需求

### 需求:每次进入重新生成地图
系统必须在每次进入地图场景时重新生成六边形地图会话，禁止复用上一局格子布局作为本局地图。

#### 场景:重新生成
- **当** 用户从培养皿进入地图
- **那么** 系统必须创建新的地图会话并生成六边形格子

### 需求:开局揭示邻格
系统必须将核心菌落放置于安全格，并自动揭示该格及其最多 6 个相邻六边形格。

#### 场景:邻格可见
- **当** 地图会话创建完成
- **那么** 玩家必须能看到核心格与相邻已揭示格，且更远处保持未揭示
