# 采集当前资源格并清空

- id: 01
- status: done
- depends: []
- maps-to-task: [1.1, 2.1, 2.2, 3.1]

## Intent

实现采集成功路径：对**核心所在格**执行采集，消耗 1 行动点与能量（第一版土壤难度 1.0 → `1 × 1.0` = 1 能量），将该格资源转入角色携带，格子变为空（资源点消失）。规则在 `systems/`，场景只提供采集操作并展示数值。

## Verify

约 30 秒内手动确认：

1. 进入地图。若核心格无资源点：点邻接已揭示的资源格走过去；若揭示区完全没有资源点，用调试把当前格设为资源（或离开再进）
2. 执行采集 → 该格资源点消失，变为空格
3. 行动点 -1；能量净 +2（耗 1、得 3）；水分 +2；营养 +2
4. 不蔓延、不结束回合验收再生（留给切片 02）

## Steps

- [x] 实现采集规则：目标为当前核心格且 `content === 'resource'`、AP 与能量足够；成功则该格改为 `empty`，扣 1 AP，返回能量消耗与产出
- [x] 地图提供明确「采集」操作（不要占用翻格/移动点选）；成功后刷新格子与 HUD
- [x] 地图 HUD 展示携带的能量/水分/营养，采集后可见变化
- [x] 开发可见调试：将当前核心格设为资源，避免开局无资源点时卡住验收
- [x] 按 Verify 走一遍

## Notes

- 第一版一次采空（二进制 `resource` → `empty`），不做格子剩余量多次采集
- 产出写死：能量 3、水分 2、营养 2；消耗能量由调用方从 `species-runtime` 扣除后再加上产出
- 与移动区分：点邻接已揭示格仍是移动；采集是当前格动作
- 验证：lint/build 通过。规则脚本：设当前格为资源后采集，AP 3→2、能量 20→22、水分 20→22、营养 10→12、该格变空。请本地进地图目视确认点消失与 HUD。
