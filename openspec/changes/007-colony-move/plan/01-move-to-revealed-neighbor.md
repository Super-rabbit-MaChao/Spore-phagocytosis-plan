# 移动到邻接已揭示格

- id: 01
- status: done
- depends: []
- maps-to-task: [1.1, 1.2, 2.1, 2.2, 3.1]

## Intent

实现核心菌落移动成功路径：点选相邻且已揭示的目标格，消耗 1 行动点与能量（第一版土壤难度 1.0 → `2 × 1.0` = 2 能量），更新核心坐标并在地图上可见标记移动。规则在 `systems/`，场景负责点选。

## Verify

约 30 秒内手动确认：

1. 进入地图（开局邻格已揭示）
2. 点击某一核心相邻的已揭示格 → 核心实心圆移到该格
3. 行动点 -1，能量 -2
4. 不翻新迷雾、不采集

## Steps

- [x] 实现移动规则：目标存在、已揭示、与核心相邻；成功则更新 `core` 并扣 1 AP + 2 能量
- [x] 地图点选合法目标格触发移动；核心标记跟新坐标渲染
- [x] HUD 反映 AP/能量变化；可高亮可移动格（可选）
- [x] 按 Verify 走一遍

## Notes

- 与翻格区分：已揭示邻格 → 移动；迷雾邻格 → 仍走翻格（若已实现）
- 难度系数本切片写死 `1.0`；移动能量公式 `2 × 难度`
- 验证：lint/build 通过。请点虚线描边的已揭示邻格，确认核心移动且 AP/能量变化。
