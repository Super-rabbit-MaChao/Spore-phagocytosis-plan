## 1. 工程脚手架

- [x] 1.1 在仓库创建 `app/`（Vite + React + TypeScript）空白工程与 `package.json` 脚本（`dev` / `build` / `preview`）
- [x] 1.2 配置基础 `index.html`、入口 `main.tsx`、路径别名（如需要）与移动端 `viewport` meta
- [x] 1.3 添加根 README 启动说明（安装、启动、访问地址）；注明本阶段无玩法逻辑
- [x] 1.4 创建空的 `src/systems/` 挂载目录及说明文件（禁止写入玩法实现）

## 2. 响应式壳层

- [x] 2.1 实现 `AppShell`（TopBar / MainViewport / BottomBar 三区布局）
- [x] 2.2 添加全局 CSS 变量与基础响应式样式（桌面宽屏 / 手机窄屏，`100dvh`）
- [x] 2.3 确保主操作集中在 BottomBar，首屏不堆叠仪表盘式多卡片

## 3. 场景占位与路由

- [x] 3.1 接入客户端路由，注册 `/`、`/petri-dish`、`/map`
- [x] 3.2 实现主菜单场景占位（标题 + 进入培养皿）
- [x] 3.3 实现培养皿场景占位（分区文案，无恢复/投喂逻辑）
- [x] 3.4 实现地图场景占位（主视区空框，无翻格/回合逻辑）
- [x] 3.5 实现场景间导航，并在 TopBar 显示当前场景名

## 4. 验收

- [x] 4.1 本地 `install` + `dev` 可打开根页面
- [x] 4.2 用桌面宽度与窄屏宽度（或手机模式）各走通：主菜单 → 培养皿 → 地图 → 返回
- [x] 4.3 确认工程中无回合/资源/进化/孢子化等玩法代码
