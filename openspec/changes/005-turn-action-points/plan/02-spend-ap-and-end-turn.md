# 扣点与结束回合

- id: 02
- status: done
- depends: [01]
- maps-to-task: [1.2, 2.2, 3.2]

## Intent

提供可复用的 `spendActionPoint`（每次扣 1，不足则拒绝）与 `endTurn`（行动点重置为 3，回合序号 +1）。地图 BottomBar 提供「结束回合」；因本变更尚无真实动作，用调试「消耗 1 AP」验收扣点接口。

## Verify

约 30 秒内手动确认：

1. 进地图 AP=`3`，点消耗 1 AP → 变为 `2`，再扣至 `0`
2. AP=`0` 时再消耗 → 仍为 `0`（拒绝）
3. 点「结束回合」→ AP 回到 `3`（回合序号若显示则 +1）
4. 无环境维持结算、无翻格/移动副作用

## Steps

- [x] 实现 `spendActionPoint`：成功扣 1；AP≤0 时拒绝且不变更
- [x] 实现 `endTurn`：AP 重置为 3，`turnIndex` +1（本阶段不做完整结算表）
- [x] BottomBar（或等价底栏操作）提供「结束回合」
- [x] 提供调试「消耗 1 AP」按钮（可仅开发可见）以便验收扣点
- [x] 按 Verify 走一遍

## Notes

- 真实翻格/移动在 `006`/`007` 再绑定同一扣点接口
- 结束回合不在本切片实现能量/水分维持消耗
- 验证：lint/build 通过；规则 3→0 拒绝、结束回 3/回合+1。请 `pnpm dev` 用地图调试按钮与底栏验收。
